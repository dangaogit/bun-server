{
  "name": "benchmark",
  "private": true,
  "scripts": {
    "bench:router": "bun router.bench.ts",
    "bench:di": "bun di.bench.ts",
    "bench:interceptor": "bun interceptor.bench.ts",
    "bench:optimization": "bun optimization.bench.ts",
    "bench:regression": "bun regression.bench.ts",
    "bench:all": "bun bench:router && bun bench:di && bun bench:interceptor && bun bench:optimization && bun bench:regression",
    "bench:wrk": "bun run-wrk.ts",
    "bench:wrk-cluster": "bun run-wrk-cluster.ts",
    "bench:wrk-worker": "bun run-wrk-worker.ts",
    "bench:compare": "bun run-wrk-compare.ts",
    "bench:wrk-nginx": "bun run-wrk-nginx.ts"
  },
  "dependencies": {
    "@dangao/bun-server": "workspace:^",
    "@dangao/logsmith": "workspace:^",
    "@nestjs/common": "catalog:",
    "@nestjs/core": "catalog:",
    "@nestjs/platform-express": "catalog:",
    "express": "catalog:",
    "rxjs": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}